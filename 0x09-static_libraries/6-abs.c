#include "main.h"

/**
 * _abs - gives absolute value of an integer
 * @n: integer
 *
 * Return: returns absolute value of i
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (n * -1);
	}
	else
	{
		return (n);
	}
}
